# Vite dev server proxy target. Set this to the deployed Modal URL when the
# backend is remote. The browser still connects to /__h3 locally, avoiding CORS.
H3_BACKEND_URL=http://127.0.0.1:7860

# Browser-side Gradio source. Keep /__h3 for local development through Vite.
VITE_H3_BACKEND=/__h3
